{% extends 'base.html.twig' %}

{% block title %}Dossier{% endblock %}

{% block body %}
    <h1>Dossier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dossier.id }}</td>
            </tr>
            <tr>
                <th>Clinique</th>
                <td>{{ dossier.clinique }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ dossier.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ dossier.lastname }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ dossier.age }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ dossier.sexe }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ dossier.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ dossier.adresse }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ dossier.phone }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ dossier.profession }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ dossier.taille }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ dossier.poids }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ dossier.createdAt ? dossier.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_dossier_index') }}">back to list</a>

    <a href="{{ path('app_admin_dossier_edit', {'id': dossier.id}) }}">edit</a>

    {{ include('admin_dossier/_delete_form.html.twig') }}
{% endblock %}
