{% extends 'base.html.twig' %}

{% block title %}Consultation{% endblock %}

{% block body %}
    <h1>Consultation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultation.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ consultation.createdAt ? consultation.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ consultation.motif }}</td>
            </tr>
            <tr>
                <th>Traitant</th>
                <td>{{ consultation.traitant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminconsultation_index') }}">back to list</a>

    <a href="{{ path('app_adminconsultation_edit', {'id': consultation.id}) }}">edit</a>

    {{ include('adminconsultation/_delete_form.html.twig') }}
{% endblock %}
