{% extends 'base.html.twig' %}

{% block title %}Edit Dossier{% endblock %}

{% block body %}
<div class="wrapper">
    {% include '_partials/asidebarInf.html.twig' %}
    <div class="main">
        {% include '_partials/_navbar.html.twig' %}
        <div class="container">
            <h1 class="text-center mt-2">Editer le Dossier</h1>

            {{ include('infirmier_dossier/_form.html.twig', {'button_label': 'Update'}) }}

        </div>
    </div>
</div>


    {# <a href="{{ path('app_infirmier_dossier_index') }}">back to list</a> #}

    {# {{ include('infirmier_dossier/_delete_form.html.twig') }} #}
{% endblock %}
