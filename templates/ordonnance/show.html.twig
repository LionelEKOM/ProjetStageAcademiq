{% extends 'base.html.twig' %}

{% block title %}Ordonnance{% endblock %}

{% block body %}
    <h1>Ordonnance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordonnance.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ordonnance.createdAt ? ordonnance.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ordonnance.title }}</td>
            </tr>
            <tr>
                <th>Medicament</th>
                <td>{{ ordonnance.medicament }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ordonnance.quantite }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ ordonnance.debut ? ordonnance.debut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ ordonnance.fin ? ordonnance.fin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ordonnance_index') }}">back to list</a>

    <a href="{{ path('app_ordonnance_edit', {'id': ordonnance.id}) }}">edit</a>

    {{ include('ordonnance/_delete_form.html.twig') }}
{% endblock %}
